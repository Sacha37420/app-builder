version: "3.9"

networks:
  sso-net:
    external: true
    name: sso-lab_sso-net
  dev-net:
    external: true
    name: dev-net

services:

  backend:
    build: ./backend
    container_name: app-builder-backend
    restart: unless-stopped
    env_file: .env
    ports:
      - "8087:8000"
    networks:
      - sso-net
      - dev-net
    volumes:
      - ./backend:/app
    labels:
      caddy: "${DOMAIN}"
      caddy.handle_path: "/app-builder-api/*"
      caddy.handle_path.reverse_proxy: "{{upstreams 8000}}"

  frontend:
    build: ./frontend
    container_name: app-builder-frontend
    restart: unless-stopped
    env_file: .env
    ports:
      - "4205:80"
    networks:
      - sso-net
    volumes:
      - ./frontend/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./frontend/nginx-entrypoint.sh:/docker-entrypoint.d/40-env-config.sh:ro
    labels:
      caddy: "${DOMAIN}"
      caddy.handle_path: "/app-builder/*"
      caddy.handle_path.reverse_proxy: "{{upstreams 80}}"
